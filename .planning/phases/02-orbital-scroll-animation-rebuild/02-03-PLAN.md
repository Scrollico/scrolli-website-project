---
phase: 02-orbital-scroll-animation-rebuild
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - components/ui/scroll-morph-hero.tsx
  - app/pricing/page.tsx
autonomous: false

requirements:
  - SCROLL-01
  - SCROLL-02
  - SCROLL-03
  - SCROLL-04
  - SCROLL-05
  - SCROLL-06
  - SCROLL-07

must_haves:
  truths:
    - "Scrolling the pricing page shows cards orbiting in a circular path — no freeze, no stutter"
    - "Scrolling back up reverses the animation smoothly"
    - "Normal scrolling resumes after animation completes without jarring jump"
    - "prefers-reduced-motion shows static grid"
    - "No overflow:visible !important or pricing-scrollytelling class on documentElement"
  artifacts:
    - path: "components/ui/scroll-morph-hero.tsx"
      provides: "Complete GSAP ScrollTrigger orbital animation"
      contains: "ScrollTrigger"
    - path: "app/pricing/page.tsx"
      provides: "Pricing page with integrated animation"
      contains: "IntroAnimation"
  key_links:
    - from: "app/pricing/page.tsx"
      to: "components/ui/scroll-morph-hero.tsx"
      via: "import IntroAnimation"
      pattern: "import.*IntroAnimation.*scroll-morph-hero"
---

<objective>
Final integration verification and visual checkpoint. Ensure the rebuilt animation integrates cleanly with the pricing page, verify no regressions in the surrounding page layout (RevenueCatPricing above, BentoGrid below), and get user approval on the animation feel.

Purpose: The animation is rebuilt but needs human eyes to confirm it feels right — the scroll distance, morph speed, arc radius, and card sizing are all subjective qualities that need visual approval.

Output: User-verified orbital scroll animation on the pricing page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-orbital-scroll-animation-rebuild/02-CONTEXT.md
@.planning/phases/02-orbital-scroll-animation-rebuild/02-01-SUMMARY.md
@.planning/phases/02-orbital-scroll-animation-rebuild/02-02-SUMMARY.md
@components/ui/scroll-morph-hero.tsx
@app/pricing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up pricing page integration and verify no layout regressions</name>
  <files>app/pricing/page.tsx, components/ui/scroll-morph-hero.tsx</files>
  <read_first>
    - app/pricing/page.tsx (verify IntroAnimation import and wrapper are correct, check z-index layering with adjacent sections)
    - components/ui/scroll-morph-hero.tsx (verify export signature matches import, check section height and pin behavior)
  </read_first>
  <action>
    1. **Verify pricing page import is unchanged:**
       `app/pricing/page.tsx` should still have:
       ```typescript
       import IntroAnimation from '@/components/ui/scroll-morph-hero'
       ```
       And the usage:
       ```tsx
       <div className="w-full relative">
         <IntroAnimation articles={animationArticles} />
       </div>
       ```
       If the wrapper `<div>` has any z-index or overflow styles that could interfere with ScrollTrigger pin, remove them. The wrapper should be plain `w-full relative` with no overflow hidden.

    2. **Check z-index layering:**
       The sections above (RevenueCatPricing, `z-50`) and below (BentoGrid, `z-50`) use `z-50`. The ScrollTrigger pinned container must NOT have a z-index that places it above these sections when unpinned. ScrollTrigger adds `position: fixed` during pin and removes it after — verify no explicit z-index on the animation section conflicts.

       If needed, add `z-10` to the pinned container so it sits below the z-50 sections:
       ```tsx
       // In scroll-morph-hero.tsx, the pin container:
       <div ref={pinContainerRef} className={cn("h-screen w-full overflow-hidden ... z-10", ...)}>
       ```

    3. **Verify the component builds without errors:**
       Run `npx next build` or at minimum `npx tsc --noEmit` to catch any type errors.

    4. **Remove any dead imports** in scroll-morph-hero.tsx — if Framer Motion imports remain from the old code, remove them. The component should only import from:
       - `react`
       - `gsap` and `gsap/ScrollTrigger`
       - `@/lib/utils`
       - `@/lib/design-tokens`
       - `@/types/content`
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/pricing/page.tsx contains `import IntroAnimation from '@/components/ui/scroll-morph-hero'`
    - app/pricing/page.tsx wrapper div does NOT contain `overflow-hidden` or `overflow: hidden`
    - components/ui/scroll-morph-hero.tsx does NOT contain `from "framer-motion"` import
    - components/ui/scroll-morph-hero.tsx does NOT contain `from "react"` importing `useState` (should only need useRef, useEffect, useMemo, useCallback)
    - `npx tsc --noEmit` exits with 0 errors related to scroll-morph-hero.tsx or pricing/page.tsx
  </acceptance_criteria>
  <done>
    Pricing page integrates cleanly with rebuilt scroll-morph-hero component. No dead imports, no z-index conflicts, no overflow issues. Full TypeScript project compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of orbital scroll animation</name>
  <files>components/ui/scroll-morph-hero.tsx, app/pricing/page.tsx</files>
  <action>
    Present the completed animation for user review. Start dev server if not running (`npm run dev`). Guide user through verification steps for forward scroll, reverse scroll, handoff, reduced-motion fallback, and performance feel.
  </action>
  <verify>User approves the animation visually</verify>
  <done>User types "approved" confirming animation works correctly</done>
  <what-built>
    Complete orbital scroll animation rebuild for the pricing page:
    - GSAP ScrollTrigger pin+scrub replaces broken wheel event architecture
    - Cards scatter, morph into circle formation, then sweep through 180-degree arc
    - All driven by native scrollbar — no scroll hijacking
    - Bidirectional: scrolling up reverses smoothly
    - prefers-reduced-motion shows static grid
    - No global DOM mutations (no pricing-scrollytelling class)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to the pricing page (http://localhost:3000/pricing)
    2. Scroll down past the pricing cards section — the animation section should appear
    3. **Test forward scroll:** Cards should scatter, form a circle, then sweep through an arc as you scroll
    4. **Test reverse scroll:** Scroll back up — animation should smoothly reverse
    5. **Test handoff:** Scroll past the animation section — page should resume normal scrolling to the bento grid below, with no jump or freeze
    6. **Test reduced motion:** In browser DevTools, enable "Emulate CSS media feature prefers-reduced-motion: reduce" (under Rendering tab). Reload page — should show a static grid of article cards, no animation
    7. **Check for issues:** Open DevTools console — no errors should appear. Check Elements tab — `<html>` element should NOT have a `pricing-scrollytelling` class
    8. **Performance feel:** Does the animation feel smooth at 60fps? Is the scroll distance (300vh) comfortable? Are the cards the right size?
  </how-to-verify>
  <resume-signal>Type "approved" if the animation looks and feels correct, or describe any issues (e.g., "cards too small", "scroll distance too long", "jank on mobile")</resume-signal>
</task>

</tasks>

<verification>
1. Full pricing page loads without errors at /pricing
2. Scrolling through animation section is smooth and bidirectional
3. No console errors related to ScrollTrigger or GSAP
4. `document.documentElement.classList` does not contain `pricing-scrollytelling`
5. Reduced-motion fallback renders static grid
6. Sections above and below animation render correctly (no z-index bleed)
</verification>

<success_criteria>
- User visually confirms the animation works correctly
- Forward scroll: scatter -> circle -> arc sweep
- Reverse scroll: smooth reversal
- Completion handoff: no jump or freeze
- Reduced motion: static grid shown
- No console errors
- 60fps feel confirmed by user
</success_criteria>

<output>
After completion, create `.planning/phases/02-orbital-scroll-animation-rebuild/02-03-SUMMARY.md`
</output>
