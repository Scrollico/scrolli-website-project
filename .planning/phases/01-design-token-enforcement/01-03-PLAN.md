---
phase: 01-design-token-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/ui/account-menu.tsx
  - components/ui/account-settings.tsx
  - components/ui/alara-wp-banner.tsx
  - components/ui/alara-wp-button.tsx
  - components/ui/animated-characters-login-page.tsx
  - components/ui/badge.tsx
  - components/ui/card.tsx
  - components/ui/cinematic-theme-switcher.tsx
  - components/ui/corporate-subscription-cta.tsx
  - components/ui/dialog.tsx
  - components/ui/drawer.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/expanding-cards-demo.tsx
autonomous: true
requirements: [TOKEN-02, TOKEN-06]

must_haves:
  truths:
    - "All 13 UI components (account-menu through expanding-cards-demo) use design token spacing instead of hardcoded py-N/px-N/p-N/gap-N"
    - "All 13 components use color tokens instead of bare bg-white/text-black without dark variants"
  artifacts:
    - path: "components/ui/badge.tsx"
      provides: "Token-migrated badge with badge.padding"
      contains: "badge\\.padding"
    - path: "components/ui/card.tsx"
      provides: "Token-migrated card"
      contains: "componentPadding"
    - path: "components/ui/dialog.tsx"
      provides: "Token-migrated dialog"
      contains: "from .@/lib/design-tokens."
  key_links:
    - from: "components/ui/badge.tsx"
      to: "lib/design-tokens.ts"
      via: "badge.padding import"
      pattern: "badge"
    - from: "components/ui/card.tsx"
      to: "lib/design-tokens.ts"
      via: "componentPadding import"
      pattern: "componentPadding"
---

<objective>
Migrate UI components batch 1 (account-menu through expanding-cards-demo) from hardcoded spacing/color classes to design tokens.

Purpose: Per D-04 (migrate ALL components) and D-05 (both spacing AND color tokens). This is the first of two UI component batches, keeping file count under 15 for reliable execution.

Output: All 13 listed UI components use design token imports for spacing and color.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts

<interfaces>
From lib/design-tokens.ts — token mapping reference for migration:

SPACING TOKENS (replace hardcoded py-N/px-N/p-N/gap-N):
- sectionPadding: { xs: "py-4 md:py-6", sm: "py-6 md:py-8", md: "py-8 md:py-12 lg:py-16", lg: "py-12 md:py-16 lg:py-20", xl: "py-16 md:py-20 lg:py-24", "2xl": "py-20 md:py-24 lg:py-32" }
- containerPadding: { xs: "px-2 sm:px-4", sm: "px-4 sm:px-6", md: "px-4 sm:px-6 lg:px-8", lg: "px-4 sm:px-6 lg:px-8 xl:px-12", xl: "px-6 sm:px-8 lg:px-12 xl:px-16" }
- componentPadding: { xs: "p-2", sm: "p-3", md: "p-4 md:p-6", lg: "p-6 md:p-8", xl: "p-8 md:p-10" }
- gap: { xs: "gap-1", sm: "gap-2", md: "gap-3", lg: "gap-4", xl: "gap-6", "2xl": "gap-8" }

COLOR TOKENS (replace hardcoded bg-white/text-black/etc):
- colors.background: { base: "bg-background", elevated: "bg-muted", overlay: "bg-background/80 backdrop-blur-md" }
- colors.foreground: { primary: "text-foreground", secondary: "text-muted-foreground", muted: "text-muted-foreground/80" }
- colors.border: { DEFAULT: "border-border", light: "border-border/50", subtle: "border-border/30" }

BADGE TOKEN (for badge.tsx specifically):
- badge.padding: "px-3 py-1.5" (line 542-544 in design-tokens.ts)

MIGRATION RULES:
- py-4/py-6 -> sectionPadding.xs or .sm (for section-level wrappers)
- py-8/py-10/py-12 -> sectionPadding.md or .lg
- px-4/px-6/px-8 -> containerPadding.sm or .md
- p-2/p-3/p-4/p-6 -> componentPadding.xs/.sm/.md/.lg
- gap-2/gap-3/gap-4/gap-6/gap-8 -> gap.sm/.md/.lg/.xl/.2xl
- bg-white -> colors.background.base
- bg-gray-50/bg-gray-100 -> colors.background.elevated
- text-gray-900/text-black -> colors.foreground.primary
- text-gray-500/text-gray-600 -> colors.foreground.secondary
- border-gray-200 -> colors.border.DEFAULT or .light

EXCEPTIONS (do NOT migrate):
- Spacing inside animation/transform calculations (pixel values for position/size)
- Skeleton placeholder colors (bg-gray-200 dark:bg-gray-700 pairs are intentional)
- Classes that already have proper dark: counterparts
- Micro-spacing like space-y-1, space-x-2 (no token equivalent)
- Inline style objects with pixel values
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate UI components batch 1 (A-E) to design tokens</name>
  <files>components/ui/account-menu.tsx, components/ui/account-settings.tsx, components/ui/alara-wp-banner.tsx, components/ui/alara-wp-button.tsx, components/ui/animated-characters-login-page.tsx, components/ui/badge.tsx, components/ui/card.tsx, components/ui/cinematic-theme-switcher.tsx, components/ui/corporate-subscription-cta.tsx, components/ui/dialog.tsx, components/ui/drawer.tsx, components/ui/dropdown-menu.tsx, components/ui/expanding-cards-demo.tsx</files>
  <read_first>lib/design-tokens.ts, each file being modified (read before editing)</read_first>
  <action>
For each of the 13 UI component files listed:

1. Read the file
2. Import the needed design tokens at the top (add to existing import or create new one):
   ```typescript
   import { sectionPadding, containerPadding, componentPadding, gap, colors, badge } from "@/lib/design-tokens";
   ```
   Only import the tokens actually used in that file.

3. Replace hardcoded spacing classes with token equivalents using the mapping in the interfaces block above. Use `cn()` to compose token classes with other classes.

4. Replace hardcoded color classes (bg-white, text-black, bg-gray-N without dark: pair, etc.) with color token equivalents.

5. Per D-05: Migrate BOTH spacing AND color tokens in each file.
6. Per D-09: Ensure the resulting spacing looks visually appropriate — choose the token size that best matches the intent.

7. Ensure `cn()` is imported from `@/lib/utils` if not already.

Key files with specific guidance:
- `badge.tsx`: Has `px-2.5 py-0.5` — use `badge.padding` token ("px-3 py-1.5") for default variant. The badge token is defined at line 542-544 of design-tokens.ts.
- `card.tsx`: Likely has `p-6` or similar — use `componentPadding.md` or `componentPadding.lg`.
- `dialog.tsx` / `drawer.tsx` / `dropdown-menu.tsx`: Radix UI-based — only migrate custom classNames, not Radix utility classes.
- `corporate-subscription-cta.tsx`: Migrate section padding and colors to tokens.
- `expanding-cards-demo.tsx`: Migrate spacing, preserve animation-related values.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit components/ui/badge.tsx components/ui/card.tsx components/ui/dialog.tsx components/ui/corporate-subscription-cta.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 13 UI component files import at least one token from `@/lib/design-tokens`
    - `badge.tsx` uses `badge.padding` token (not hardcoded `px-2.5 py-0.5`)
    - `grep -rn "bg-white" components/ui/account-menu.tsx components/ui/account-settings.tsx components/ui/alara-wp-banner.tsx components/ui/card.tsx components/ui/dialog.tsx components/ui/drawer.tsx` returns zero results
    - TypeScript compilation passes for all modified files
  </acceptance_criteria>
  <done>All 13 UI components (batch 1) use design tokens for spacing and color. No bare bg-white/text-black remains in these files.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all 13 modified files
2. `grep -rn "bg-white" components/ui/account-menu.tsx components/ui/account-settings.tsx components/ui/alara-wp-banner.tsx components/ui/alara-wp-button.tsx components/ui/animated-characters-login-page.tsx components/ui/badge.tsx components/ui/card.tsx components/ui/cinematic-theme-switcher.tsx components/ui/corporate-subscription-cta.tsx components/ui/dialog.tsx components/ui/drawer.tsx components/ui/dropdown-menu.tsx components/ui/expanding-cards-demo.tsx` returns zero results
3. `grep -c "badge\\.padding" components/ui/badge.tsx` returns at least 1
4. Dark mode check: `grep -rn "bg-white\|text-black" components/ui/account-menu.tsx components/ui/card.tsx components/ui/dialog.tsx | grep -v "dark:" | wc -l` returns 0
</verification>

<success_criteria>
- All 13 UI components migrated to design tokens (TOKEN-02 partial)
- badge.tsx uses badge.padding token
- No bare bg-white/text-black remains (TOKEN-06 partial)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-03-SUMMARY.md`
</output>
