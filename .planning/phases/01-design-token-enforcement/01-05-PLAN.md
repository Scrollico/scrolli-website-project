---
phase: 01-design-token-enforcement
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/layout/footer/Footer.tsx
  - components/layout/header/CardNav.tsx
  - components/layout/header/Header.tsx
  - components/layout/header/MobileSidebar.tsx
  - components/layout/header/StickyNav.tsx
  - components/layout/header/UserMenu.tsx
  - components/elements/Pagination.tsx
  - components/responsive/ResponsiveGrid.tsx
  - components/responsive/Stack.tsx
autonomous: true
requirements: [TOKEN-02, TOKEN-06]

must_haves:
  truths:
    - "All layout, elements, and responsive components use design token spacing instead of hardcoded py-N/px-N/p-N/gap-N"
    - "All 9 components use color tokens instead of bare bg-white/text-black without dark variants"
  artifacts:
    - path: "components/layout/footer/Footer.tsx"
      provides: "Token-migrated footer"
      contains: "sectionPadding\\|containerPadding\\|componentPadding\\|gap"
    - path: "components/layout/header/Header.tsx"
      provides: "Token-migrated header"
      contains: "from .@/lib/design-tokens."
  key_links:
    - from: "components/layout/footer/Footer.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
    - from: "components/layout/header/Header.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
---

<objective>
Migrate layout, elements, and responsive components from hardcoded spacing/color classes to design tokens.

Purpose: Per D-04 (migrate ALL components). Layout components (header, footer, navigation) are high-visibility and must use consistent tokens.

Output: All 9 layout/elements/responsive components use design token imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts

<interfaces>
From lib/design-tokens.ts — token mapping reference:

SPACING TOKENS:
- sectionPadding: { xs: "py-4 md:py-6", sm: "py-6 md:py-8", md: "py-8 md:py-12 lg:py-16", lg: "py-12 md:py-16 lg:py-20", xl: "py-16 md:py-20 lg:py-24" }
- containerPadding: { xs: "px-2 sm:px-4", sm: "px-4 sm:px-6", md: "px-4 sm:px-6 lg:px-8", lg: "px-4 sm:px-6 lg:px-8 xl:px-12" }
- componentPadding: { xs: "p-2", sm: "p-3", md: "p-4 md:p-6", lg: "p-6 md:p-8", xl: "p-8 md:p-10" }
- gap: { xs: "gap-1", sm: "gap-2", md: "gap-3", lg: "gap-4", xl: "gap-6", "2xl": "gap-8" }

COLOR TOKENS:
- colors.background: { base: "bg-background", elevated: "bg-muted", overlay: "bg-background/80 backdrop-blur-md", navbar: "bg-navbar dark:bg-background" }
- colors.navbarBeige: { DEFAULT: "bg-navbarBeige dark:bg-background" }
- colors.foreground: { primary: "text-foreground", secondary: "text-muted-foreground" }
- colors.border: { DEFAULT: "border-border", light: "border-border/50", subtle: "border-border/30" }

EXCEPTIONS (do NOT migrate):
- Structural heights (h-16, h-20) — these are intentional fixed heights for navigation
- Animation pixel values
- Classes already correctly paired with dark: variants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout, elements, and responsive components to design tokens</name>
  <files>components/layout/footer/Footer.tsx, components/layout/header/CardNav.tsx, components/layout/header/Header.tsx, components/layout/header/MobileSidebar.tsx, components/layout/header/StickyNav.tsx, components/layout/header/UserMenu.tsx, components/elements/Pagination.tsx, components/responsive/ResponsiveGrid.tsx, components/responsive/Stack.tsx</files>
  <read_first>lib/design-tokens.ts, each file being modified (read before editing)</read_first>
  <action>
For each of the 9 component files:

1. Read the file
2. Import needed design tokens (only those actually used)
3. Replace hardcoded spacing and color classes per mapping rules
4. Per D-05 and D-06: Both spacing and color tokens, verify light/dark mode correctness

Component-specific guidance:
- `Footer.tsx`: Section-level — use `sectionPadding` for vertical, `containerPadding` for horizontal. Footer bg should use `colors.background.base` or `colors.navbarBeige.DEFAULT`.
- `Header.tsx` / `StickyNav.tsx`: Navigation — use `containerPadding` for horizontal spacing. Keep structural heights (h-16, h-20) as-is.
- `MobileSidebar.tsx`: Modal-like — use `componentPadding` for internal spacing.
- `CardNav.tsx`: Card navigation — use `componentPadding` and `gap` tokens.
- `UserMenu.tsx`: Dropdown — use `componentPadding` for items.
- `Pagination.tsx`: Use `gap` for button spacing.
- `ResponsiveGrid.tsx` / `Stack.tsx`: May already use token-like patterns — migrate any remaining hardcoded values.

Per D-09: Spacing should feel visually balanced.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit components/layout/footer/Footer.tsx components/layout/header/Header.tsx components/layout/header/StickyNav.tsx components/elements/Pagination.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 component files import at least one token from `@/lib/design-tokens`
    - `grep -rn "bg-white\|text-black" components/layout/ components/elements/ components/responsive/ | grep -v "dark:" | wc -l` returns 0
    - `grep -c "sectionPadding\|containerPadding\|componentPadding\|gap" components/layout/footer/Footer.tsx` returns at least 1
    - TypeScript compilation passes for all modified files
  </acceptance_criteria>
  <done>All 9 layout/elements/responsive components use design tokens. No bare bg-white/text-black remains.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all 9 modified files
2. `grep -rn "bg-white\|text-black" components/layout/ components/elements/ components/responsive/ | grep -v "dark:" | wc -l` returns 0
3. `grep -rn "from \"@/lib/design-tokens\"" components/layout/ components/elements/ components/responsive/ | wc -l` >= 8
</verification>

<success_criteria>
- All 9 layout/elements/responsive components migrated to design tokens (TOKEN-02 partial)
- No bare bg-white/text-black without dark variant (TOKEN-06 partial)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-05-SUMMARY.md`
</output>
