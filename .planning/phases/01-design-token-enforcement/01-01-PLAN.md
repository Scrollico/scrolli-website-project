---
phase: 01-design-token-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/design-tokens.ts
  - components/ui/button.tsx
  - components/ui/bento-grid.tsx
  - components/ui/scroll-morph-hero.tsx
  - app/pricing/page.tsx
autonomous: true
requirements: [TOKEN-01, TOKEN-03, TOKEN-04]

must_haves:
  truths:
    - "colors.border.subtle resolves to 'border-border/30' — not undefined"
    - "Bento card borders are visually rendered using colors.border.subtle"
    - "Button size variants use button.padding tokens instead of hardcoded px/py values"
    - "PricingSkeleton, bento-grid, scroll-morph-hero, and pricing page.tsx use token-exported classes for all spacing and color"
  artifacts:
    - path: "lib/design-tokens.ts"
      provides: "colors.border.subtle token"
      contains: "subtle"
    - path: "components/ui/button.tsx"
      provides: "Token-based button sizing"
      contains: "button.padding"
    - path: "components/ui/bento-grid.tsx"
      provides: "Token-based bento grid"
      contains: "colors.border.subtle"
    - path: "app/pricing/page.tsx"
      provides: "Token-based pricing page"
      contains: "sectionPadding"
  key_links:
    - from: "components/ui/bento-grid.tsx"
      to: "lib/design-tokens.ts"
      via: "colors.border.subtle import"
      pattern: "colors\\.border\\.subtle"
    - from: "components/ui/button.tsx"
      to: "lib/design-tokens.ts"
      via: "button.padding import"
      pattern: "button\\.padding"
    - from: "app/pricing/page.tsx"
      to: "lib/design-tokens.ts"
      via: "sectionPadding and containerPadding imports"
      pattern: "sectionPadding|containerPadding"
---

<objective>
Fix the broken border token, migrate button sizing to token system, and migrate all pricing page components to use design tokens for spacing and color.

Purpose: Establish the foundation — fix TOKEN-01 (broken border), TOKEN-04 (button spacing), and TOKEN-03 (pricing components) so the token system is correct and the pricing page is fully token-based before the broader migration sweep.

Output: Fixed design-tokens.ts with `colors.border.subtle`, token-based button sizes, and all 4 pricing page components fully migrated to design tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts
@components/ui/button.tsx
@components/ui/bento-grid.tsx
@components/ui/scroll-morph-hero.tsx
@app/pricing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix border.subtle token and migrate button sizing to token system</name>
  <files>lib/design-tokens.ts, components/ui/button.tsx</files>
  <read_first>lib/design-tokens.ts, components/ui/button.tsx</read_first>
  <action>
**In `lib/design-tokens.ts`:**

1. Add `subtle` key to `colors.border` object (per D-01, D-02). Insert after the existing `light` key at line ~338:
```typescript
subtle: "border-border/30",
```
The full `colors.border` object should become:
```typescript
border: {
  DEFAULT: "border-border",
  light: "border-border/50",
  subtle: "border-border/30",
  medium: "border-border",
  strong: "border-gray-400 dark:border-gray-400",
  hover: "hover:border-border/80",
},
```

2. Add `button.height` tokens to the `button` object (per D-08). Add after `button.padding`:
```typescript
height: {
  sm: "h-8",
  md: "h-10",
  lg: "h-12",
  icon: "h-10 w-10",
},
```

**In `components/ui/button.tsx`:**

3. Update the import to include `button` from design-tokens (per D-07):
```typescript
import { elevation, buttonPairs, button } from "@/lib/design-tokens";
```

4. Replace hardcoded size variants in the CVA `size` object. Current values:
```typescript
size: {
  default: "h-10 px-4 py-2",
  sm: "h-8 rounded-md px-3 text-xs",
  lg: "h-12 rounded-md px-8 text-base",
  icon: "h-10 w-10",
},
```
Replace with token-based values:
```typescript
size: {
  default: `${button.height.md} ${button.padding.md}`,
  sm: `${button.height.sm} rounded-md ${button.padding.sm} text-xs`,
  lg: `${button.height.lg} rounded-md ${button.padding.lg} text-base`,
  icon: `${button.height.icon}`,
},
```
Note: Keep `rounded-md`, `text-xs`, `text-base` as-is since they are typography/radius concerns, not spacing. The `rounded-md` on sm/lg overrides the base `rounded-md` for those sizes specifically.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit lib/design-tokens.ts components/ui/button.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `lib/design-tokens.ts` contains `subtle: "border-border/30"` inside the `colors.border` object
    - `lib/design-tokens.ts` contains `height:` object inside the `button` object with keys `sm`, `md`, `lg`, `icon`
    - `components/ui/button.tsx` contains `import { elevation, buttonPairs, button } from "@/lib/design-tokens"`
    - `components/ui/button.tsx` contains `button.height.md` and `button.padding.md` in the size variants
    - `components/ui/button.tsx` does NOT contain the hardcoded string `"h-10 px-4 py-2"` in the size variants
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>colors.border.subtle resolves to "border-border/30", button sizes use token system, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Migrate pricing page components to design tokens</name>
  <files>app/pricing/page.tsx, components/ui/bento-grid.tsx, components/ui/scroll-morph-hero.tsx</files>
  <read_first>app/pricing/page.tsx, components/ui/bento-grid.tsx, components/ui/scroll-morph-hero.tsx, lib/design-tokens.ts</read_first>
  <action>
**In `app/pricing/page.tsx`:**

1. Update import to include spacing tokens:
```typescript
import { colors, sectionPadding, containerPadding, gap, componentPadding } from "@/lib/design-tokens";
```

2. Migrate `PricingSkeleton` component (line 18-41):
   - `"px-4 sm:px-6 lg:px-8 pt-10 pb-12 min-h-[600px]"` → `cn(containerPadding.md, sectionPadding.md, "min-h-[600px]")`
   - `"animate-pulse space-y-8"` → `cn("animate-pulse", gap["2xl"])` (gap.2xl = "gap-8" but space-y-8 is vertical stack spacing; keep `space-y-8` since gap tokens are for flex/grid — space-y is different). Actually keep `space-y-8` as-is since gap tokens only apply to flex/grid containers.
   - `"grid grid-cols-1 md:grid-cols-3 gap-6"` → `cn("grid grid-cols-1 md:grid-cols-3", gap.xl)` (gap.xl = "gap-6")
   - Skeleton placeholder colors `bg-gray-200 dark:bg-gray-700` → keep as-is since these are skeleton-specific decorative colors, not semantic tokens. They intentionally look "placeholder-ish."

3. Migrate pricing page sections (lines 247-269):
   - `"relative z-50 pt-16 pb-8"` → `cn("relative z-50", sectionPadding.lg)` (close to pt-16 pb-8 combined effect; sectionPadding.lg = "py-12 md:py-16 lg:py-20"). Per D-09, ensure spacing looks good visually — sectionPadding.lg provides responsive scaling.
   - `"relative z-50 py-16"` (line 259 and 266) → `cn("relative z-50", sectionPadding.xl)` (sectionPadding.xl = "py-16 md:py-20 lg:py-24" — upgrades from static py-16 to responsive)

**In `components/ui/bento-grid.tsx`:**

4. Update imports to include more tokens:
```typescript
import { colors, borderRadius, border, gap, componentPadding } from "@/lib/design-tokens"
```

5. `BentoCard` component — replace `"p-4 sm:p-8"` with `componentPadding.md` (which is `"p-4 md:p-6"`). Note: current `sm:p-8` is more generous; per D-09, use `componentPadding.lg` (`"p-6 md:p-8"`) for a better visual balance on bento cards:
```typescript
cn("relative overflow-hidden", componentPadding.lg, className)
```

6. `BentoDescription` — replace hardcoded `"mx-0 my-2 max-w-sm"` — keep `max-w-sm` but `my-2` is small vertical margin which is fine to keep (no marginY token exists at that size). Keep as-is since it's intra-component micro-spacing.

**In `components/ui/scroll-morph-hero.tsx`:**

7. Read the full file first and identify all hardcoded spacing/color values. Migrate any `py-N`, `px-N`, `gap-N`, `bg-white`, `text-black`, `bg-gray-*` without dark variants to use token equivalents. The component already imports `colors` and `typography` — extend the import to include spacing tokens as needed. For any inline pixel values used in animation calculations (width, height, position), leave as-is since they are animation math, not design spacing.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit app/pricing/page.tsx components/ui/bento-grid.tsx components/ui/scroll-morph-hero.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/pricing/page.tsx` imports `sectionPadding` from `@/lib/design-tokens`
    - `app/pricing/page.tsx` contains `sectionPadding.lg` or `sectionPadding.xl` (not bare `py-16`)
    - `app/pricing/page.tsx` contains `containerPadding.md` (not bare `px-4 sm:px-6 lg:px-8`)
    - `app/pricing/page.tsx` contains `gap.xl` (not bare `gap-6`)
    - `components/ui/bento-grid.tsx` contains `componentPadding.lg` or `componentPadding.md` (not bare `p-4 sm:p-8`)
    - `components/ui/scroll-morph-hero.tsx` has no bare `bg-white` or `text-black` without dark variant
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>All four pricing page components (PricingSkeleton, BentoAbstract, bento-grid.tsx, scroll-morph-hero.tsx) use token-exported class strings for spacing and color. No hardcoded py-N/px-N/gap-N remains in pricing components (except animation math values).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all modified files
2. `grep -n "border-border/30" lib/design-tokens.ts` returns the subtle token definition
3. `grep -n "button.padding" components/ui/button.tsx` returns token usage in size variants
4. `grep -n "sectionPadding\|containerPadding" app/pricing/page.tsx` returns token usage
5. `grep -n "componentPadding" components/ui/bento-grid.tsx` returns token usage
</verification>

<success_criteria>
- colors.border.subtle resolves to "border-border/30" (TOKEN-01 complete)
- Button size variants use button.padding and button.height tokens (TOKEN-04 complete)
- All pricing page components use design tokens for spacing/color (TOKEN-03 complete)
- TypeScript compiles without errors
- No visual regressions (components render same or better with responsive token scaling)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-01-SUMMARY.md`
</output>
