---
phase: 01-design-token-enforcement
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/premium/PaywallModal.tsx
  - components/premium/PremiumGate.tsx
  - components/premium/RevenueCatPricing.tsx
  - components/paywall/ArticleGateWrapper.tsx
  - components/paywall/EmailGateModal.tsx
  - components/paywall/GiftArticleModal.tsx
  - components/paywall/PaywallSlideUp.tsx
  - components/paywall/UsageMeter.tsx
  - components/onboarding/OnboardingForm.tsx
autonomous: true
requirements: [TOKEN-02, TOKEN-06]

must_haves:
  truths:
    - "All premium, paywall, and onboarding components use design token spacing instead of hardcoded py-N/px-N/p-N/gap-N"
    - "All 9 components use color tokens instead of bare bg-white/text-black without dark variants"
  artifacts:
    - path: "components/premium/RevenueCatPricing.tsx"
      provides: "Token-migrated pricing component"
      contains: "sectionPadding\\|containerPadding\\|componentPadding\\|gap"
    - path: "components/paywall/PaywallSlideUp.tsx"
      provides: "Token-migrated paywall slide-up"
      contains: "componentPadding"
  key_links:
    - from: "components/premium/RevenueCatPricing.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
    - from: "components/paywall/PaywallSlideUp.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
---

<objective>
Migrate premium, paywall, and onboarding components from hardcoded spacing/color classes to design tokens.

Purpose: Per D-04 (migrate ALL components). These components handle subscription and onboarding flows — consistent token usage ensures dark mode and responsive spacing work correctly in conversion-critical UI.

Output: All 9 premium/paywall/onboarding components use design token imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts

<interfaces>
From lib/design-tokens.ts — token mapping reference:

SPACING TOKENS:
- sectionPadding: { xs: "py-4 md:py-6", sm: "py-6 md:py-8", md: "py-8 md:py-12 lg:py-16", lg: "py-12 md:py-16 lg:py-20", xl: "py-16 md:py-20 lg:py-24" }
- containerPadding: { xs: "px-2 sm:px-4", sm: "px-4 sm:px-6", md: "px-4 sm:px-6 lg:px-8" }
- componentPadding: { xs: "p-2", sm: "p-3", md: "p-4 md:p-6", lg: "p-6 md:p-8", xl: "p-8 md:p-10" }
- gap: { xs: "gap-1", sm: "gap-2", md: "gap-3", lg: "gap-4", xl: "gap-6", "2xl": "gap-8" }

COLOR TOKENS:
- colors.background: { base: "bg-background", elevated: "bg-muted", overlay: "bg-background/80 backdrop-blur-md" }
- colors.foreground: { primary: "text-foreground", secondary: "text-muted-foreground" }
- colors.border: { DEFAULT: "border-border", light: "border-border/50", subtle: "border-border/30" }

EXCEPTIONS: Animation pixel values, skeleton placeholder color pairs, micro-spacing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate premium, paywall, and onboarding components to design tokens</name>
  <files>components/premium/PaywallModal.tsx, components/premium/PremiumGate.tsx, components/premium/RevenueCatPricing.tsx, components/paywall/ArticleGateWrapper.tsx, components/paywall/EmailGateModal.tsx, components/paywall/GiftArticleModal.tsx, components/paywall/PaywallSlideUp.tsx, components/paywall/UsageMeter.tsx, components/onboarding/OnboardingForm.tsx</files>
  <read_first>lib/design-tokens.ts, each file being modified (read before editing)</read_first>
  <action>
For each of the 9 component files:

1. Read the file
2. Import needed design tokens (only those actually used)
3. Replace hardcoded spacing and color classes per mapping rules
4. Per D-05 and D-06: Both spacing and color tokens, verify light/dark mode correctness

Component-specific guidance:
- `RevenueCatPricing.tsx`: Key pricing component — use `sectionPadding`, `containerPadding`, `gap` tokens. Critical for TOKEN-03.
- `PaywallModal.tsx` / `PaywallSlideUp.tsx` / `EmailGateModal.tsx` / `GiftArticleModal.tsx`: Modal components — use `componentPadding` for content areas.
- `PremiumGate.tsx` / `ArticleGateWrapper.tsx`: Gate components — use `sectionPadding` for wrappers, `componentPadding` for card-like content.
- `UsageMeter.tsx`: Small component — use `componentPadding` and `gap`.
- `OnboardingForm.tsx`: Form — use `componentPadding` for form sections, `gap` for field spacing.

Per D-09: Spacing should feel visually balanced.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit components/premium/RevenueCatPricing.tsx components/paywall/PaywallSlideUp.tsx components/onboarding/OnboardingForm.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 component files import at least one token from `@/lib/design-tokens`
    - `grep -rn "bg-white\|text-black" components/premium/ components/paywall/ components/onboarding/ | grep -v "dark:" | wc -l` returns 0
    - `grep -c "sectionPadding\|containerPadding\|componentPadding\|gap" components/premium/RevenueCatPricing.tsx` returns at least 1
    - TypeScript compilation passes for all modified files
  </acceptance_criteria>
  <done>All 9 premium/paywall/onboarding components use design tokens. No bare bg-white/text-black remains.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all 9 modified files
2. `grep -rn "bg-white\|text-black" components/premium/ components/paywall/ components/onboarding/ | grep -v "dark:" | wc -l` returns 0
3. `grep -rn "from \"@/lib/design-tokens\"" components/premium/ components/paywall/ components/onboarding/ | wc -l` >= 8
</verification>

<success_criteria>
- All 9 premium/paywall/onboarding components migrated (TOKEN-02 partial)
- No bare bg-white/text-black without dark variant (TOKEN-06 partial)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-06-SUMMARY.md`
</output>
