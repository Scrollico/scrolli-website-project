---
phase: 01-design-token-enforcement
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/ui/expanding-cards.tsx
  - components/ui/follow-button.tsx
  - components/ui/LoadingSkeletons.tsx
  - components/ui/modal.tsx
  - components/ui/pill-morph-tabs.tsx
  - components/ui/podcast-gallery.tsx
  - components/ui/pricing-section-1.tsx
  - components/ui/scroll-indicator.tsx
  - components/ui/sidebar-news.tsx
  - components/ui/simple-premium-cta.tsx
  - components/ui/tabs.tsx
  - components/ui/textarea.tsx
autonomous: true
requirements: [TOKEN-02, TOKEN-06]

must_haves:
  truths:
    - "All 12 UI components (expanding-cards through textarea) use design token spacing instead of hardcoded py-N/px-N/p-N/gap-N"
    - "All 12 components use color tokens instead of bare bg-white/text-black without dark variants"
  artifacts:
    - path: "components/ui/LoadingSkeletons.tsx"
      provides: "Token-migrated loading skeletons"
      contains: "sectionPadding\\|containerPadding\\|componentPadding\\|gap"
    - path: "components/ui/pricing-section-1.tsx"
      provides: "Token-migrated pricing section"
      contains: "from .@/lib/design-tokens."
    - path: "components/ui/modal.tsx"
      provides: "Token-migrated modal"
      contains: "componentPadding"
  key_links:
    - from: "components/ui/LoadingSkeletons.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
    - from: "components/ui/pricing-section-1.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
---

<objective>
Migrate UI components batch 2 (expanding-cards through textarea) from hardcoded spacing/color classes to design tokens.

Purpose: Per D-04 (migrate ALL components) and D-05 (both spacing AND color tokens). This is the second of two UI component batches.

Output: All 12 listed UI components use design token imports for spacing and color.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts

<interfaces>
From lib/design-tokens.ts — token mapping reference for migration:

SPACING TOKENS (replace hardcoded py-N/px-N/p-N/gap-N):
- sectionPadding: { xs: "py-4 md:py-6", sm: "py-6 md:py-8", md: "py-8 md:py-12 lg:py-16", lg: "py-12 md:py-16 lg:py-20", xl: "py-16 md:py-20 lg:py-24", "2xl": "py-20 md:py-24 lg:py-32" }
- containerPadding: { xs: "px-2 sm:px-4", sm: "px-4 sm:px-6", md: "px-4 sm:px-6 lg:px-8", lg: "px-4 sm:px-6 lg:px-8 xl:px-12", xl: "px-6 sm:px-8 lg:px-12 xl:px-16" }
- componentPadding: { xs: "p-2", sm: "p-3", md: "p-4 md:p-6", lg: "p-6 md:p-8", xl: "p-8 md:p-10" }
- gap: { xs: "gap-1", sm: "gap-2", md: "gap-3", lg: "gap-4", xl: "gap-6", "2xl": "gap-8" }

COLOR TOKENS (replace hardcoded bg-white/text-black/etc):
- colors.background: { base: "bg-background", elevated: "bg-muted", overlay: "bg-background/80 backdrop-blur-md" }
- colors.foreground: { primary: "text-foreground", secondary: "text-muted-foreground", muted: "text-muted-foreground/80" }
- colors.border: { DEFAULT: "border-border", light: "border-border/50", subtle: "border-border/30" }

MIGRATION RULES:
- py-4/py-6 -> sectionPadding.xs or .sm
- py-8/py-10/py-12 -> sectionPadding.md or .lg
- px-4/px-6/px-8 -> containerPadding.sm or .md
- p-2/p-3/p-4/p-6 -> componentPadding.xs/.sm/.md/.lg
- gap-2/gap-3/gap-4/gap-6/gap-8 -> gap.sm/.md/.lg/.xl/.2xl
- bg-white -> colors.background.base
- bg-gray-50/bg-gray-100 -> colors.background.elevated
- text-gray-900/text-black -> colors.foreground.primary
- text-gray-500/text-gray-600 -> colors.foreground.secondary
- border-gray-200 -> colors.border.DEFAULT or .light

EXCEPTIONS (do NOT migrate):
- Spacing inside animation/transform calculations
- Skeleton placeholder colors (bg-gray-200 dark:bg-gray-700 pairs are intentional)
- Classes that already have proper dark: counterparts
- Micro-spacing like space-y-1, space-x-2
- Inline style objects with pixel values
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate UI components batch 2 (E-T) to design tokens</name>
  <files>components/ui/expanding-cards.tsx, components/ui/follow-button.tsx, components/ui/LoadingSkeletons.tsx, components/ui/modal.tsx, components/ui/pill-morph-tabs.tsx, components/ui/podcast-gallery.tsx, components/ui/pricing-section-1.tsx, components/ui/scroll-indicator.tsx, components/ui/sidebar-news.tsx, components/ui/simple-premium-cta.tsx, components/ui/tabs.tsx, components/ui/textarea.tsx</files>
  <read_first>lib/design-tokens.ts, each file being modified (read before editing)</read_first>
  <action>
For each of the 12 UI component files listed:

1. Read the file
2. Import needed design tokens (only those actually used):
   ```typescript
   import { sectionPadding, containerPadding, componentPadding, gap, colors } from "@/lib/design-tokens";
   ```

3. Replace hardcoded spacing classes with token equivalents per the mapping rules.
4. Replace hardcoded color classes with token equivalents.
5. Per D-05: Migrate BOTH spacing AND color tokens.
6. Per D-09: Choose token sizes that feel visually appropriate.
7. Ensure `cn()` is imported from `@/lib/utils` if not already.

Key files with specific guidance:
- `LoadingSkeletons.tsx`: Migrate section/container padding. Keep skeleton placeholder colors (bg-gray-200 dark:bg-gray-700) as-is — they are intentional.
- `pricing-section-1.tsx`: Migrate all spacing and colors to tokens.
- `modal.tsx`: Use `componentPadding` for internal spacing.
- `tabs.tsx`: Only migrate custom spacing, preserve Radix data-state classes.
- `textarea.tsx`: Migrate padding, preserve form-specific focus ring styles.
- `simple-premium-cta.tsx` / `sidebar-news.tsx`: Migrate section padding and colors.
- `expanding-cards.tsx`: Preserve animation pixel values, migrate non-animation spacing.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit components/ui/LoadingSkeletons.tsx components/ui/modal.tsx components/ui/pricing-section-1.tsx components/ui/simple-premium-cta.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 12 UI component files import at least one token from `@/lib/design-tokens`
    - `grep -rn "py-[0-9]\|px-[0-9]\|p-[0-9]\|gap-[0-9]" components/ui/LoadingSkeletons.tsx` returns only exceptions (skeleton colors, micro-spacing)
    - `grep -rn "bg-white" components/ui/expanding-cards.tsx components/ui/LoadingSkeletons.tsx components/ui/modal.tsx components/ui/pricing-section-1.tsx components/ui/sidebar-news.tsx components/ui/simple-premium-cta.tsx | grep -v "dark:" | wc -l` returns 0
    - TypeScript compilation passes for all modified files
  </acceptance_criteria>
  <done>All 12 UI components (batch 2) use design tokens for spacing and color. No bare bg-white/text-black remains.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all 12 modified files
2. `grep -rn "bg-white\|text-black" components/ui/expanding-cards.tsx components/ui/follow-button.tsx components/ui/LoadingSkeletons.tsx components/ui/modal.tsx components/ui/pill-morph-tabs.tsx components/ui/podcast-gallery.tsx components/ui/pricing-section-1.tsx components/ui/scroll-indicator.tsx components/ui/sidebar-news.tsx components/ui/simple-premium-cta.tsx components/ui/tabs.tsx components/ui/textarea.tsx | grep -v "dark:" | wc -l` returns 0
3. `grep -rn "from \"@/lib/design-tokens\"" components/ui/LoadingSkeletons.tsx components/ui/pricing-section-1.tsx` returns at least 2 results
</verification>

<success_criteria>
- All 12 UI components migrated to design tokens (TOKEN-02 partial)
- No bare bg-white/text-black remains (TOKEN-06 partial)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-04-SUMMARY.md`
</output>
