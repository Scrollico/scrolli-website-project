---
phase: 01-design-token-enforcement
plan: 09
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/page.tsx
  - app/[slug]/page.tsx
  - app/collabs/page.tsx
  - app/error.tsx
  - app/gift/[token]/GiftRedemptionClient.tsx
  - app/global-error.tsx
  - app/not-found.tsx
  - app/onboarding/page.tsx
  - app/profile/page.tsx
  - app/stories/page.tsx
  - app/subscribe/SubscribePageContent.tsx
  - app/test-paywall/page.tsx
autonomous: true
requirements: [TOKEN-02, TOKEN-06]

must_haves:
  truths:
    - "All 12 app pages use design token spacing instead of hardcoded py-N/px-N/p-N/gap-N"
    - "All 12 app pages use color tokens instead of bare bg-white/text-black without dark variants"
    - "Full codebase dark mode compliance verified — no bg-white/text-black without dark: counterparts across all migrated files"
  artifacts:
    - path: "app/page.tsx"
      provides: "Token-migrated homepage"
      contains: "sectionPadding\\|containerPadding"
    - path: "app/error.tsx"
      provides: "Token-migrated error page"
      contains: "from .@/lib/design-tokens."
  key_links:
    - from: "app/page.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
    - from: "app/[slug]/page.tsx"
      to: "lib/design-tokens.ts"
      via: "token imports"
      pattern: "from .@/lib/design-tokens."
---

<objective>
Migrate all app pages from hardcoded spacing/color classes to design tokens, then run full codebase verification to confirm TOKEN-02 and TOKEN-06 are complete.

Purpose: Per D-04 (migrate ALL components in a single sweep). This is the final migration batch — after this, the full codebase uses design tokens consistently.

Output: All 12 app page files use design tokens. Full TOKEN-02 migration complete. TOKEN-06 dark mode compliance verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-enforcement/01-CONTEXT.md
@lib/design-tokens.ts
@lib/utils.ts

<interfaces>
From lib/design-tokens.ts — token mapping reference:

SPACING TOKENS:
- sectionPadding: { xs: "py-4 md:py-6", sm: "py-6 md:py-8", md: "py-8 md:py-12 lg:py-16", lg: "py-12 md:py-16 lg:py-20", xl: "py-16 md:py-20 lg:py-24" }
- containerPadding: { xs: "px-2 sm:px-4", sm: "px-4 sm:px-6", md: "px-4 sm:px-6 lg:px-8" }
- componentPadding: { xs: "p-2", sm: "p-3", md: "p-4 md:p-6", lg: "p-6 md:p-8" }
- gap: { xs: "gap-1", sm: "gap-2", md: "gap-3", lg: "gap-4", xl: "gap-6", "2xl": "gap-8" }

COLOR TOKENS:
- colors.background: { base: "bg-background", elevated: "bg-muted" }
- colors.foreground: { primary: "text-foreground", secondary: "text-muted-foreground" }
- colors.border: { DEFAULT: "border-border", light: "border-border/50", subtle: "border-border/30" }

MIGRATION RULES:
- app/page.tsx (homepage): Use `sectionPadding` for section wrappers, `containerPadding` for content width gutters
- app/[slug]/page.tsx (article detail): Use `sectionPadding` for content areas
- Error/not-found pages: Use `sectionPadding` for centering, `colors.background.base` for background
- Form pages (onboarding, profile): Use `sectionPadding` and `componentPadding`
- bg-white -> colors.background.base; text-black -> colors.foreground.primary

EXCEPTIONS: Animation pixel values, skeleton color pairs, micro-spacing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate app pages to design tokens</name>
  <files>app/page.tsx, app/[slug]/page.tsx, app/collabs/page.tsx, app/error.tsx, app/gift/[token]/GiftRedemptionClient.tsx, app/global-error.tsx, app/not-found.tsx, app/onboarding/page.tsx, app/profile/page.tsx, app/stories/page.tsx, app/subscribe/SubscribePageContent.tsx, app/test-paywall/page.tsx</files>
  <read_first>lib/design-tokens.ts, each file being modified (read before editing)</read_first>
  <action>
For each of the 12 app page files:

1. Read the file
2. Import needed design tokens
3. Replace hardcoded spacing with token equivalents
4. Replace hardcoded color classes with token equivalents

Page-specific guidance:
- **app/page.tsx** (homepage): Main entry — use `sectionPadding` for section wrappers, `containerPadding` for content width gutters.
- **app/[slug]/page.tsx** (article detail): Use `sectionPadding` for content areas.
- **app/collabs/page.tsx**, **app/stories/page.tsx**: Collection pages — `sectionPadding` and `containerPadding`.
- **app/error.tsx**, **app/not-found.tsx**, **app/global-error.tsx**: Error pages — `sectionPadding` for centering, `colors.background.base` for background.
- **app/onboarding/page.tsx**: Form page — `sectionPadding` and `componentPadding`.
- **app/profile/page.tsx**: User page — `sectionPadding` and `componentPadding`.
- **app/subscribe/SubscribePageContent.tsx**: Subscription flow — `sectionPadding` and `containerPadding`.
- **app/gift/[token]/GiftRedemptionClient.tsx**: Gift flow — `componentPadding`.
- **app/test-paywall/page.tsx**: Test page — basic token migration.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit app/page.tsx app/error.tsx app/not-found.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 12 app page files import at least one token from `@/lib/design-tokens`
    - `grep -c "sectionPadding\|containerPadding\|componentPadding\|gap\|colors" app/page.tsx` returns at least 1
    - `grep -rn "from \"@/lib/design-tokens\"" app/ --include="*.tsx" | wc -l` >= 10
    - TypeScript compilation passes for all modified files
  </acceptance_criteria>
  <done>All 12 app pages migrated to design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Full codebase dark mode and token verification</name>
  <files></files>
  <action>
Run comprehensive verification across all migrated files to confirm TOKEN-02 and TOKEN-06 are complete:

1. **Dark mode compliance check (TOKEN-06):** Run grep-based static check to verify no bg-white/text-black without dark: counterparts across all component and app directories:
```bash
grep -rn "bg-white\|text-black" components/ app/ --include="*.tsx" | grep -v "dark:" | grep -v "node_modules" | grep -v ".next/"
```
This should return 0 results. If any results appear, fix them in this task.

2. **Token import coverage check (TOKEN-02):** Verify all active component files import from design-tokens:
```bash
grep -rn "from \"@/lib/design-tokens\"" components/ui/ components/layout/ components/sections/ components/premium/ components/paywall/ components/onboarding/ components/elements/ components/responsive/ app/ --include="*.tsx" | wc -l
```
This should return >= 75 (roughly matching total migrated file count).

3. **ESLint verification:** Run lint to confirm no error-level regressions:
```bash
npx next lint 2>&1 | grep -c "error"
```
This should return 0 (only "warn" level from the design token plugin).

4. **Build verification:** Confirm Next.js builds successfully:
```bash
npm run build 2>&1 | tail -10
```
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && echo "=== Dark mode check ===" && grep -rn "bg-white\|text-black" components/ app/ --include="*.tsx" | grep -v "dark:" | grep -v "node_modules" | grep -v ".next/" | wc -l && echo "=== Token imports ===" && grep -rn "from \"@/lib/design-tokens\"" components/ app/ --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "bg-white\|text-black" components/ app/ --include="*.tsx" | grep -v "dark:" | grep -v "node_modules" | grep -v ".next/" | wc -l` returns 0
    - `grep -rn "from \"@/lib/design-tokens\"" components/ app/ --include="*.tsx" | wc -l` returns >= 75
    - `npx next lint 2>&1 | grep "error" | grep -v "no-hardcoded" | wc -l` returns 0
    - `npm run build` exits successfully
  </acceptance_criteria>
  <done>Full codebase TOKEN-02 migration verified. TOKEN-06 dark mode compliance confirmed — no bg-white/text-black without dark: counterparts. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all modified files
2. `grep -rn "bg-white\|text-black" components/ app/ --include="*.tsx" | grep -v "dark:" | grep -v "node_modules" | grep -v ".next/" | wc -l` returns 0 (TOKEN-06)
3. `grep -rn "from \"@/lib/design-tokens\"" components/ app/ --include="*.tsx" | wc -l` >= 75 (TOKEN-02)
4. `npx next lint 2>&1 | grep "error" | grep -v "no-hardcoded" | wc -l` returns 0
5. `npm run build` exits successfully
</verification>

<success_criteria>
- All 12 app pages migrated to design tokens (TOKEN-02 complete)
- Full codebase dark mode compliance verified (TOKEN-06 complete)
- Next.js build passes
- ESLint shows only warn-level token violations (no errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-enforcement/01-09-SUMMARY.md`
</output>
