---
phase: quick
plan: 260323-wj3
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/smart-button.tsx
  - components/paywall/PaywallSlideUp.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All subscribe/CTA buttons show charcoal bg (#374152) + white text in light mode"
    - "All subscribe/CTA buttons show beige bg (#F8F5E4) + near-black text (#111827) in dark mode"
    - "Legacy CSS cannot override button colors because inline styles take precedence"
    - "PaywallSlideUp green CTA button uses inline styles for green in both modes"
  artifacts:
    - path: "components/ui/smart-button.tsx"
      provides: "Inline-style color injection for charcoal/beige variants"
    - path: "components/paywall/PaywallSlideUp.tsx"
      provides: "Inline-styled green CTA button"
  key_links:
    - from: "components/ui/smart-button.tsx"
      to: "components/ui/login-button.tsx"
      via: "style prop passed through to Button"
      pattern: "style=\\{"
---

<objective>
Fix all subscribe/CTA buttons to use inline styles for background and text colors, matching the navbar Subscribe button pattern. Legacy CSS overrides Tailwind classes on these buttons, making them invisible or wrong-colored. Inline styles are immune to CSS specificity wars.

Purpose: Ensure CTA buttons are visually correct in both light and dark mode regardless of legacy CSS.
Output: SmartButton component injects inline styles; PaywallSlideUp green button also hardened.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@components/ui/smart-button.tsx
@components/ui/login-button.tsx
@components/ui/button.tsx
@components/layout/header/UserMenu.tsx (reference pattern lines 70-83)
@components/paywall/PaywallSlideUp.tsx
@lib/design-tokens.ts (buttonPairs.charcoal, buttonPairs.beige)

<interfaces>
<!-- The fix chain: SmartButton -> LoginButton -> Button -->
<!-- SmartButton already has isDark state via MutationObserver -->

From components/ui/smart-button.tsx:
```typescript
// SmartButton determines variant: isDark ? "beige" : "dark"
// Then calls LoginButton which maps:
//   "brand-charcoal" | "dark" | "charcoal" -> "default" variant
//   "beige" | "brand-beige" -> "brand-beige" variant
// Button applies Tailwind classes from buttonPairs tokens
// PROBLEM: Legacy CSS overrides these Tailwind classes
```

From components/layout/header/UserMenu.tsx (REFERENCE PATTERN):
```typescript
// Navbar Subscribe button uses inline style to bypass CSS:
style={{
  backgroundColor: isDark ? '#F8F5E4' : '#374152',
  color: isDark ? '#111827' : '#ffffff',
}}
```

Color mapping:
- Light mode charcoal: bg=#374152, text=#ffffff
- Dark mode beige: bg=#F8F5E4, text=#111827
- PaywallSlideUp green: bg=#16a34a (green-600), text=#ffffff (both modes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline style injection to SmartButton for charcoal/beige variants</name>
  <files>components/ui/smart-button.tsx</files>
  <action>
Modify SmartButton to pass a `style` prop with inline backgroundColor and color for the auto-detected or forced charcoal/beige variants. This fixes ALL SmartButton consumers at once (ScrolliPremiumBanner, PaywallModal, RevenueCatPricing, etc.).

Implementation:
1. After computing `variant` (line ~54), determine inline colors based on variant:
   - If variant resolves to "dark", "charcoal", or "brand-charcoal" (charcoal variants):
     - `backgroundColor: isDark ? '#F8F5E4' : '#374152'`
     - `color: isDark ? '#111827' : '#ffffff'`
   - If variant resolves to "beige" or "brand-beige" (beige variants):
     - `backgroundColor: '#F8F5E4'`
     - `color: '#111827'` (beige is always cream bg with dark text)
   - For all other variants (outline, ghost, link, success, periwinkle, etc.): no inline style override

2. Create a `brandStyle` object (or undefined) based on the above logic.

3. Pass `style={brandStyle}` to the LoginButton component (both the asChild and regular render paths).

4. In LoginButton (login-button.tsx), accept and forward a `style` prop to Button. Check if Button already spreads `...props` which would include style -- it does via `{...props}` in button.tsx line 75. So LoginButton just needs to ensure it passes `style` through, which it already does via `{...props}` on line 37. No changes needed to login-button.tsx or button.tsx.

5. The Tailwind bg/text classes from buttonPairs will still apply but inline styles have higher specificity than everything, so colors will always be correct.

Important: Do NOT change the `isDark` detection logic (MutationObserver pattern). Only add the style computation and passing.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>SmartButton passes inline backgroundColor/color for charcoal and beige variants. TypeScript compiles without errors. All SmartButton consumers (ScrolliPremiumBanner, PaywallModal, RevenueCatPricing, newsletter banners, etc.) automatically get inline-styled colors.</done>
</task>

<task type="auto">
  <name>Task 2: Harden PaywallSlideUp green CTA with inline styles</name>
  <files>components/paywall/PaywallSlideUp.tsx</files>
  <action>
PaywallSlideUp uses a raw `<button>` with Tailwind `bg-green-600 hover:bg-green-700 text-white`. This is a different CTA style (green, not charcoal/beige) but is still vulnerable to legacy CSS overrides.

At the `<button>` element near line 378:
1. Add an inline `style` prop: `style={{ backgroundColor: '#16a34a', color: '#ffffff' }}`
2. Keep the existing Tailwind classes for layout, rounding, font, transitions, disabled states, and flex -- only the color classes (`bg-green-600 text-white`) become redundant but leave them as fallback.
3. Remove `hover:bg-green-700` from className (inline bg overrides it anyway) and add `hover:opacity-90` for a subtle hover dim effect consistent with the charcoal button pattern.

This ensures the green CTA renders correctly regardless of legacy CSS interference.
  </action>
  <verify>
    <automated>cd /Volumes/max/DevS/scrolli.co && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>PaywallSlideUp green CTA button uses inline styles for bg/text color. Hover uses opacity dim. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npm run build` completes (if feasible in context)
3. Visual spot-check: All subscribe buttons show correct charcoal/beige colors in both light and dark mode
</verification>

<success_criteria>
- SmartButton injects inline backgroundColor/color for charcoal and beige variants
- All SmartButton consumers inherit the fix without individual file changes
- PaywallSlideUp green button hardened with inline styles
- No TypeScript errors introduced
- Legacy CSS can no longer override CTA button colors
</success_criteria>

<output>
After completion, create `.planning/quick/260323-wj3-standardize-subscribe-cta-buttons-to-inl/260323-wj3-SUMMARY.md`
</output>
